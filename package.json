{
  "name": "workflow-zagonel",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "lint": "node --test",
    "dev": "wrangler pages dev public --ip 0.0.0.0 --port 3000",
    "build": "mkdir -p dist && cp -r public/* dist/ && wrangler pages functions build functions --outdir dist",
    "deploy": "wrangler pages deploy public"
  },
  "devDependencies": {
    "wrangler": "^4.132.0"
  }
}
